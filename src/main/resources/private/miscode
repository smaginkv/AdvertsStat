    //use when jms sent structure
    @Bean
    public JmsListenerContainerFactory<?> myFactory(ConnectionFactory connectionFactory,
                                                    DefaultJmsListenerContainerFactoryConfigurer configurer) {
        DefaultJmsListenerContainerFactory factory = new DefaultJmsListenerContainerFactory();
        // This provides all boot's default to this factory, including the message converter
        configurer.configure(factory, connectionFactory);
        // You could still override some of Boot's default if necessary.
        return factory;
    }

    @Bean // Serialize message content to json using TextMessage
    public MessageConverter jacksonJmsMessageConverter() {
        MappingJackson2MessageConverter converter = new MappingJackson2MessageConverter();
        converter.setTargetType(MessageType.OBJECT);
        converter.setTypeIdPropertyName("_type");
        return converter;
    }

//connection to the separate data base server
#spring.datasource.url=jdbc:sqlserver://bazait18;databaseName=parse_8_skv
#spring.datasource.username=usr1c
#spring.datasource.password=1
#spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
#spring.jpa.show-sql=true
#spring.jpa.hibernate.dialect=org.hibernate.dialect.SQLServer2012Dialect

//connection to the message server
#spring.artemis.host=localhost
#spring.artemis.port=8161
#spring.artemis.user=admin
#spring.artemis.password=123
#spring.artemis.embedded.enabled=false
#spring.jms.pub-sub-domain=true